import 'package:flutter/material.dart';

class DessertPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Desert'),
      ),
      body: Center(
        child: Text('Desert Page'),
      ), 
      
    );
  }
}